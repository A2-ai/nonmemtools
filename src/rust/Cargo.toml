[package]
name = 'nonmemtools'
version = '0.1.0'
edition = '2021'

[lib]
# need rlib so can pull into integration tests
crate-type = [ 'staticlib', 'rlib' ]
name = 'nonmemtools'
path = "src/lib.rs"

[dependencies]
extendr-api = '*'
itertools = "0.12.1"
nom = "7.1.3"
